extends base.pug


block content
    h4 Change Title of video
    form(action="")
        input(placeholder="Video Title",value=video.title,repuired)
        input(value="Save", type="submit")
